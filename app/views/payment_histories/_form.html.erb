<%= form_with(model: payment_history) do |form| %>
  <% if payment_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_history.errors.count, "error") %> prohibited this payment_history from being saved:</h2>

      <ul>
        <% payment_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
